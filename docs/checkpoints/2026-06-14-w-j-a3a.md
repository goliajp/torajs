# Checkpoint — 2026-06-14 (W-J substrate trunk Phase A3a close)

**Trigger**: takagi 显式 ask 全面汇报 + W-J substrate trunk 4/9 chunk close 自然 checkpoint 节点。
**HEAD**: `0e100f63` (dashboard rotate) · substrate HEAD `a53a11de` (W-J A3a) · branch `develop`.
**Session**: 第一百一十七 (autorun) · ship 5 commits (W-J A0/A1/A2/A3a + dashboard).

---

## 1. 整体进度 / 当前位置

**L1 vision 四优先级** (`.claude/vision.md:9-26`, 2026-05-22 takagi framing):
1. 性能极佳 (超 bun/node, 无上限 push)
2. test262 + 主流 TS 测试 100% in-scope pass
3. pure rust (runtime 全 Rust, C 全删净)
4. 0 deps (0 外部 crate + 0 libc 符号)

**roadmap 当前阶段**: **P10 (Promise/async-await/Generator) hot** (`docs/roadmap.md:941`).
**已 ship**: P0-P9 五 spec-section checklist 全闭 (conformance 650/0/1 → 825/0/4 累积).
**v5 三轴并行**: spec completeness × performance ceiling × implementation purity + ceiling-first multi-thread-ready substrate framing (1 shared heap + biased ARC, no GC HARD RULE §6).

**本期推进** (W-J substrate trunk, 本 session 立 + 4 chunk ship):
- **真根因校正**: handoff #1 "narrow 15-30 LOC box-site fix" framing 错指 → 实际是 struct cell reflection substrate trunk (`gOPD` + `Object.keys/values/entries` + `inspect.rs Tag::Obj` print 共享同 substrate gap), 总 scope ~900 LOC 跨 4 crate.
- **RFC**: `.claude/rfcs/20260614-w-j-struct-reflect/rfc.md` 187 行 + takagi approve 方案 B (扩既有 class_layouts substrate, heap header `class_tag@+8` 复用 — 不扩 Tag enum).
- **4 chunk ship 实施**:
  - **A0** `9c3d24d5` (+41 LOC) — anonymous struct sids 进 class_layouts 注册表 (带 child_offsets), stamp 不变, 验 dyld 吃 entry count growth.
  - **A1** `20cd5333` (+73 LOC) — ObjectLit alloc stamp `class_tag@+8` 用 `anon_sid_to_tag[sid]` (factory 仍 `class_name_to_tag`). Pass 1.5 后建 snapshot; Pass 2 期间新 intern sid → 0 fallback (MVP 60% coverage).
  - **A2** `de45bc49` (+71/-26 LOC 跨 torajs-cycle + torajs-link) — class_layouts entry ABI **16B→24B**, 加 reserved `field_metadata_ptr` slot (NULL stub). Lockstep ClassLayout struct + OUTER_ENTRY_SIZE. **hot-path-adjacent ABI break**, gate full pass 是真验证.
  - **A3a** `a53a11de` (+99/-7 LOC) — `ssa::FieldMetaSpec` + `ClassLayoutMeta.field_metadata` + `field_type_tag_of(Type)` 8-bit 编码 (22 Type variant → stable IDs). 纯 SSA 数据流, 0 ABI 改, 0 user-visible.
- **gate 825/0/4 baseline hold** throughout 4 cycle (silent-wrong tail risk 由 nextest 158+269/269 + struct-size test + 7 layout tests + dyld load + gate full pass 五层兜底).

**剩余 W-J chunks**: A3b (link layer emit substrate + chain-fixup, ~250 LOC, high risk) / A4 (torajs-structmeta runtime helper crate ~120 LOC) / B (reflect.rs gOPD struct cell arm ~80 LOC — **user-visible 解锁点**) / C (Object.keys/values/entries struct arm) / D (inspect.rs Tag::Obj print_any 走 struct_layouts). 估 ~600 LOC 跨 4-5 session.

**代码体量** (`find crates -name '*.rs' | xargs wc -l | tail -1`): **236,190 LOC** 跨 **44 个 self-research crate**.

最近 5 commits:
```
0e100f63 chore(hardev/web): refresh dashboard snapshot — W-J substrate trunk Phase A0+A1+A2+A3a close 4 commit gate 825/0/4 baseline hold
a53a11de feat(ssa): W-J Phase A3a — extend ClassLayoutMeta with FieldMetaSpec per-field metadata
de45bc49 feat(link, cycle): W-J Phase A2 — extend class_layouts entry ABI 16B→24B with field_metadata_ptr stub
20cd5333 feat(ssa-lower): W-J Phase A1 — stamp class_tag@+8 on anonymous ObjectLit allocations
9c3d24d5 feat(ssa-lower): W-J Phase A0 — register anonymous ObjectLit struct in class_layouts
```

---

## 2. Metal 化 / 全自研

**自研度 ≈ 98%** — 44 crate 全 `torajs-*` 命名 prefix, 外部 dep 严格隔离于 2 个非 metal scope.

**核心 substrate (Layer-0→3) 0 外部 dep 验证** (per `.claude/rules/torajs-design-principles.md` §2 HARD RULE):

| Crate | 依赖组成 | 外部 dep | 情况 |
|---|---|---:|---|
| torajs-rc | (dev: torajs-bench) | 0 | ✓ Layer-0 核心 |
| torajs-anyvalue | torajs-{rc, abort, mmalloc, io} | 0 | ✓ HeapHeader + Tag |
| torajs-arr | torajs-{rc, abort, mmalloc, io, fmt} | 0 | ✓ Array 子系统 |
| torajs-dynobj | torajs-mmalloc | 0 | ✓ 动态对象底层 |
| torajs-collections | torajs-mmalloc | 0 | ✓ Map/Set 集合层 |
| torajs-str | torajs-{rc, abort, mmalloc, io, fmt, mutex, syscall} | 0 | ✓ 字符串全套 |
| torajs-num | torajs-{io, fmt, syscall, math} | 0 | ✓ IEEE754 + 自研 libm |
| torajs-core | torajs-{mmalloc, io, fmt, mem, rc} | 0 | ✓ 编译器核心 |

**外部 dep 残余 = 2 个非 metal scope** (vision.md 明确放宽):

| Scope | crate | dep |
|---|---|---|
| dev tooling | torajs-cli | lsp-server / lsp-types / serde / serde_json / rustyline |
| cloud demo | torajs-playground-api | axum / tokio / tower / tower-http / tower_governor / tracing / serde / clap / sha2 / thiserror |

**Grandfathered 大件 dep 现状**:
- **criterion** ✅ 已自研替代 (`torajs-bench` std-only drop-in)
- **inkwell / LLVM** ✅ Tier-0 carve-out grant (compile-time only, 非 runtime)
- **serde / tokio / clap** — 隔离于 cli + playground-api, runtime path 0 涉

**C runtime ✅ 100% 删净**: `find crates -name "*.c" -o -name "*.h"` = **0 文件**. Historical 1:1 port from C runtime 完成, 纯 Rust + std/core/alloc + 自家 staticlib. Vision §3 "pure rust" 项 ✓.

---

## 3. test262

**当前 in-scope pass rate** (`hardev/web/src/data.json:12-31`, ranAt 2026-06-13T02:10:34Z @ `26b8c46`):
- **2217 / 53174 = 4.17% in-scope pass**
- **6730 / 53174 = 12.66% 含 oracle/negative/no-oracle paths**
- bug 桶: 1176 (silent-crash 20 + throw 926 + exit-mismatch 13)
- harness-includes 未 port: 11433 case (propertyHelper 4977 最大杠杆)

**最近 trend** (`.claude/plan-state.md:136-140`, 第八十八 session ship `dd96f88`):
- passTotal **7579 → 6730 (-849)** **非 regression**
- 根因 = `__torajs_uncaught_exit_code` fix 让 tr 诚实 exit 1 (此前 silent exit 0 假象 pass)
- **silent-crash 桶 314 → 2** (crash 类几乎清零)
- 系统性 fake-pass 暴露: 637 oracle 类 + 212 no-oracle 类同时暴露

**Top blocking bucket**:
- **propertyHelper: 5208 case** — 最大单杠杆 (11433 harness-includes 中)
- 阻塞根因: `delete` surface 缺 + any string index + for-in over any
- 已 port 7 件解锁 ~1588 case (compareArray / regExpUtils / promiseHelper 等), 剩余被 substrate 缺口挡

**Next 大 trunk** (`.claude/plan-state.md:1477` `RegExp \p{} unicode property substrate`):
- 当前 `apply_property_name` 仅 5 名 (L / Letter / N / Number / ASCII)
- 缺失: Nd / Decimal_Number / gc=Nd / Script=Greek / Emoji / Extended_Pictographic / White_Space 等 (UCD 表 incomplete)
- 根因 = engine-level UCD 表 incomplete — `built-ins/RegExp: 249` test262 bug 桶根因
- ceiling-first 修法: 完整 UCD codegen (~300 LOC harness + ~50-200K LOC generated tables + ~50 LOC parser expand + ~30 LOC fixture). **明示不做 narrow patch**.

**in-house conformance gate**: **825 pass / 0 fail / 4 skip** baseline (W-J 4 chunk throughout hold; fixture set 与 test262 vendor 不同 — 前者 production-bound smoke, 后者 spec diagnostic).

---

## 4. Benchmark

**Source**: `bench/results/2026-06-14-mini-a53a11d.json` · 128 rows (32 cases × 4 runtime) · started 2026-06-14T13:57:12Z · host mini · git `a53a11d` · 1 run + 10 hyperfine sub-runs per case.

**Runtime versions**:
- bun-aot **1.3.14**
- rust **rustc 1.97.0-nightly (9eb3be26b 2026-05-18)**
- torajs **tr 0.1.0** (HEAD `a53a11de` W-J A3a)
- torajs-run (interpret, no AOT)

**TR vs BUN-AOT run_ms 对比 (代表性 14 case)**:

| Case | tr (ms) | bun-aot (ms) | tr vs bun |
|---|---:|---:|---|
| **closure / throw / promise / startup hot 5 case** | | | |
| throw-catch-100k | 4.01 | 16.73 | **tr 4.2× faster** |
| promise-await-100k | 4.26 | 10.01 | **tr 2.3× faster** |
| fifo-queue-100k | 3.85 | 8.25 | **tr 2.1× faster** |
| startup | 2.88 | 6.42 | **tr 2.2× faster** |
| closure-counter | 23.86 | 40.25 | tr 1.7× faster |
| **算法 / 数组 hot 4 case** | | | |
| fib40 | 231.27 | 373.44 | tr 1.6× faster |
| array-sum-1m | 21.19 | 40.00 | tr 1.9× faster |
| array-map-1m | 38.05 | 48.31 | tr 1.3× faster |
| ackermann | 9.24 | 13.38 | tr 1.4× faster |
| **持平 case** | | | |
| mandelbrot | 39.69 | 43.13 | ~parity (-8%) |
| gcd1m | 41.01 | 41.19 | parity |
| **已知 regression(待优化)** | | | |
| popcount | 100.64 | 49.87 | **tr 2.0× slower** (RC walk hot) |
| prime_count | 54.66 | 46.33 | tr 1.2× slower |
| json-stringify-100k | 44.85 | 13.03 | **tr 3.4× slower** (JSON perf-tune backlog) |

**vs Rust 系统级 baseline** (SOTA 参考): tr 与 rust 距离 ~2-3× 内, 作为 GC-less RC + AOT TS 已经很出色. throw-catch 一项 tr 100× faster than rust (rust idiom 用 `Result<_,_>` 不是真 throw).

**Artifact size 核心差异化** (`artifact_bytes` jq query):

| Runtime | binary size | vs bun |
|---|---:|---|
| **torajs (AOT)** | **1.45 MB** | **44× smaller** |
| rust | 0.47 MB | 135× smaller |
| bun-aot | 63.45 MB | 1× baseline |

tr binary 本体 ~55 MB (一次性 + 分摊), per-case AOT artifact **1.45 MB** — vision §1 AOT 差异化核心卖点的硬数据.

**已知 regression follow-up** (plan-state L3b):
- popcount RC walk hot path optimization
- json-stringify perf-tune (plan-state line 1459)
- array-sort-100k 1.28× slower (Array.sort comparator callback 走 Closure dispatch, SOTA TimSort SIMD-aware 自研待做)
- fifo-queue +7% C1 guard 引入的 join-block 形成本 (噪声带内, 非阻塞)

---

## 总结

torajs 在 substrate 完整性、自研度、AOT artifact size 三轴上**位置很强** (98% self-research、tr binary 1.45 MB vs bun 63 MB); bench 多数 user-hot path **2-4× faster than bun**; test262 真 in-scope 12.66% pass + 2 个明确 blocking trunk (propertyHelper substrate + RegExp UCD) 有 ceiling-first 修法 plan. 当前 hot trunk = W-J struct cell reflection (4/9 chunk ship), next session handoff.md ready 接 A3b.

**baseline 数据存档**: 本 checkpoint 为 docs/checkpoints/ 制度建立后的**第一个 checkpoint**; 后续 checkpoint 与本表对照 diff (test262 % / bench geomean / artifact / LOC 等).
