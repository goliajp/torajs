# v0.7-A4 torajs-fmt finding — snprintf / strtod libc cutover

> **Status**: 🟡 audit + design — substrate ships pending.
> **Hotified**: 2026-05-28 (A3 phase CLOSED at `a0b994a` via Step
> 14-b/c io cutover; vs rust 1.651× / vs bun-aot 4.502× geomean
> with growing headroom).
> **Goal**: replace every libc `snprintf` (f64 → string) and
> `strtod` (string → f64) call site in tr-built user binaries
> with self-written algorithms. Outcome: A4 acceptance per cold
> plan §A 轴: "dtoa byte-identical to bun/node on 26 bench case".

## Scope

A4 covers the **f64 → string** and **string → f64** ABI surface.
Both are libc dependencies that the user binary still pulls in.
After A4: only `write(2)` (already direct syscall via
torajs-syscall) and `__torajs_libc_*` (mmalloc shim, A5 scope)
remain as user-binary libc surface.

### libc call sites (audit @ 2026-05-28, HEAD `a0b994a`)

**snprintf — f64 → string formatting:**

| # | Where | Family | Use |
|---|-------|--------|-----|
| 1 | `crates/torajs-num/src/to_str.rs:29` | `f64_shortest` | `%.0f` (integer-valued) + `%.*g` (try-precisions 1..=17) |
| 2 | `crates/torajs-arr/src/join.rs:41` | join's f64 element format | same `%.0f` / `%.*g` try-precisions pattern |
| 3 | `crates/torajs-arr/src/print.rs:?` | print's f64 element format | reuses join's `f64_shortest`-shape via local snprintf |
| 4 | `crates/torajs-panic/src/lib.rs:47+172+190+203+218` | panic message formatting (stderr path) | `%s` / `%d` / `%p` — **out of A4 scope** (stderr; A5 handles) |

**strtod — string → f64 parsing:**

| # | Where | Use |
|---|-------|-----|
| 5 | `crates/torajs-num/src/to_str.rs:30` | f64_shortest's round-trip verifier (does `%.*g` precision N round-trip to the same f64?) |
| 6 | `crates/torajs-arr/src/join.rs:42` | join's f64-format round-trip verifier (same shape) |
| 7 | `crates/torajs-str/src/json_parse/parse_float.rs:6` | `JSON.parse` numeric token → f64 |

The `to_number.rs` ToNumber spec (per ES §7.1.4) **already
ports strtod to Rust** — see comment at line 21-25. The
remaining strtod call sites use it for round-trip verification
in the f64_shortest try-precisions loop. Once Ryū / Grisu3 is
shipped (deterministic shortest-roundtrip, no try-precisions
needed), strtod's round-trip role disappears. Only `parse_float.rs`
(JSON parser) still genuinely needs string → f64; that becomes
an Eisel-Lemire substrate.

### Total user-binary libc impact (post-A3)

`otool -L /tmp/csv-trim-100k.bin` shows the user binary still
links libc dylib for:

- `_snprintf` (3 call sites — A4 scope)
- `_strtod` (3 call sites — A4 scope)
- `___torajs_libc_*` (mmalloc shim symbols — A5 scope)
- `_write` (already direct syscall via torajs-syscall, not libc
  buffered — A5 verifies)

After A4: only `___torajs_libc_*` + `_write` remain. After A5:
linker shouldn't pull `/usr/lib/libSystem.B.dylib` at all
(otool -L target: 0 libSystem entries).

## Design — new `torajs-fmt` crate

### Crate scaffold

```
crates/torajs-fmt/
├── Cargo.toml
├── src/
│   ├── lib.rs       — pub use dtoa::*; pub use atod::*;
│   ├── dtoa.rs      — Ryū f64 → string (preferred over Grisu3)
│   └── atod.rs      — Eisel-Lemire string → f64
```

### Algorithm choices

**dtoa: Ryū (Ulf Adams, 2018)** over Grisu3 / Grisu2 / dragon4

- **Why Ryū**: deterministic shortest-roundtrip in **one pass**
  (no try-precisions / round-trip verifier loop). Per Adams'
  paper: 2-3× faster than Grisu3 + handles edge cases (subnormals,
  ±0, NaN) without fallback. Same algorithm bun uses internally.
- **Why not Grisu3**: requires fallback to dragon4 on ~0.1% of
  inputs that fail the boundary check; adds complexity.
- **Why not snprintf-based try-precisions**: O(17) snprintf calls
  per print in the worst case; libc dependency; portability gap
  (subtle %g rounding differences across libcs).
- **LOC estimate**: ~500-700 prod LOC (Ryū's f64-to-string
  function + 1024-entry power-of-10 table + bit manipulation
  helpers). Smaller than Grisu3's ~800 LOC.

**atod: Eisel-Lemire (2020)** over hand-rolled strtod

- **Why Eisel-Lemire**: state-of-the-art fast-path string-to-f64
  parser (used by Rust std `f64::from_str`, simdjson). ~3-5×
  faster than strtod on the typical numeric token. Correct on
  100% of inputs (deterministic round-to-nearest-even).
- **Why not strtod port verbatim**: libc strtod's internal
  algorithm is complex (bigint mul + multi-precision compare);
  Eisel-Lemire is the modern equivalent that fits in fewer LOC.
- **LOC estimate**: ~800-1000 prod LOC (Eisel-Lemire core + 256-
  entry significand × power-of-10 table + slow-path bigint
  fallback for the ~0.0001% of inputs that don't fast-path).

### Public extern entry points

```rust
// crates/torajs-fmt/src/dtoa.rs
//
// f64 → shortest-roundtrip decimal byte representation. Writes
// into `out_buf` (must be ≥ 32 bytes for any valid f64 — see
// Ryū's max output length proof).
//
// Returns the number of bytes written. -1 on out-of-bounds /
// invalid f64 special cases that need caller handling (NaN /
// ±Infinity per JS spec: print "NaN" / "Infinity" / "-Infinity"
// literal strings, NOT a number — caller's responsibility).
#[unsafe(no_mangle)]
pub extern "C" fn __torajs_fmt_dtoa(d: f64, out_buf: *mut u8, out_cap: usize) -> i32 {
    // Ryū implementation
}

// crates/torajs-fmt/src/atod.rs
//
// String → f64 with full ES §7.1.4 ToNumber semantics on the
// input span. Returns the parsed f64; caller-provided endp is
// set to the first byte not consumed (NULL allowed if caller
// doesn't care).
#[unsafe(no_mangle)]
pub unsafe extern "C" fn __torajs_fmt_atod(s: *const u8, len: usize, endp: *mut usize) -> f64 {
    // Eisel-Lemire implementation
}
```

### Ladder order

| Ship | Scope | Risk |
|------|-------|------|
| **15-a** | torajs-fmt crate scaffold + Cargo.toml + lib.rs re-exports | Low — empty crate skeleton; no algorithm yet |
| **15-b** | Ryū dtoa core + ≥ 100 cfg(test) round-trip tests against `f64::to_string()` (Rust std's existing impl) | Medium — algorithm correctness; tests catch regressions |
| **15-c** | Eisel-Lemire atod core + ≥ 100 cfg(test) round-trip tests against `f64::from_str` | Medium — same shape as 15-b |
| **15-d** | Call site cutover: torajs-num/to_str.rs, torajs-arr/join.rs, torajs-arr/print.rs swap snprintf → __torajs_fmt_dtoa | High — every f64 print path in user binary, must verify bun-parity on ≥10 console.log(<number>) fixtures |
| **15-e** | Call site cutover: torajs-str/json_parse/parse_float.rs swap strtod → __torajs_fmt_atod | High — every JSON.parse(<number>) path |
| **15-f** | (optional) snprintf format-string survey: any remaining %s / %d / %p uses in torajs-panic stderr path → consider migrating to Rust's `core::fmt` or simple inline format helpers if A5 ends up needing this | Low — stderr only, no perf gate impact |

After 15-d + 15-e: `nm /tmp/<bench-bin> | grep -cE "(snprintf|strtod)"` == 0 for any bench fixture.

## Acceptance per ship

### 15-a — torajs-fmt crate scaffold

- New crate `torajs-fmt` in `crates/`, depends only on `core` (no
  std, no libc, no allocator). Mirror torajs-io's
  no-runtime-deps shape so torajs-fmt is consumable by torajs-num
  / torajs-arr / torajs-str without circular issues.
- `cargo build --workspace --release`: 0 error / 0 warn.
- File-size: torajs-fmt/src/*.rs all < 500 LOC.
- No callers yet — crate dormant until 15-b.

### 15-b — Ryū dtoa

- `__torajs_fmt_dtoa` public extern entry point.
- ≥ 100 cfg(test) round-trip tests: random f64 + edge cases
  (subnormals, ±0, max/min normal, NaN handling per JS spec).
  Match `f64::to_string()` byte-for-byte where Rust std is
  shortest-roundtrip; document any edge-case divergences.
- No callers yet — symbol available for 15-d.

### 15-c — Eisel-Lemire atod

- `__torajs_fmt_atod` public extern entry point.
- ≥ 100 cfg(test) round-trip tests: random f64 → str → f64
  identity check + edge cases.
- Slow-path bigint fallback for the ~0.0001% of inputs that
  don't fast-path (Eisel-Lemire's known limitation).

### 15-d — f64 → string call site cutover (substrate)

- torajs-num / torajs-arr Rust callers swap snprintf → dtoa.
- Pre-flight ALL GREEN.
- Conformance gate: **687 / 0 / 1 PASS**.
- bun-parity ≥ 10 fixtures including: `console.log(3.14)`,
  `console.log(0)`, `console.log(-0)`, `console.log(1e21)`,
  `console.log(Number.MAX_SAFE_INTEGER + 1)`, `console.log(NaN)`,
  `console.log([1.5, 2.5, 3.5].join(","))`, etc.

### 15-e — string → f64 call site cutover (substrate)

- torajs-str/json_parse Rust caller swaps strtod → atod.
- bun-parity ≥ 10 fixtures including: `JSON.parse("3.14")`,
  `JSON.parse("[1, 2.5, 1e-10]")`, etc.

### 15-f — (optional) panic stderr formatting

- Survey + design; ship only if perf-critical or A5 final libc
  audit shows residual stderr-path libc dep.

After 15-e: A4 acceptance per cold plan met (dtoa + atod
byte-identical to bun on ≥ 20 fixtures); A5 final libc verify
becomes the closing audit before A 轴 close.

## Why this is "正统"

- **Mainstream choice** — Ryū (used by bun, also citable per
  Adams' SIGPLAN '18 paper) + Eisel-Lemire (used by simdjson,
  Rust std `f64::from_str`, several modern JIT runtimes). Both
  are textbook PL-implementation choices.
- **Bun-parity acceptance** — byte-equal output is trivially
  provable on a finite fixture set; bun's own dtoa is also Ryū-
  based.
- **No silent-wrong risk** — algorithm correctness is well-
  studied; both have published proofs + reference implementations
  in multiple languages (C / Java / Go / Rust). torajs-fmt port
  is a translation, not a research project.
- **Single substrate boundary per cutover** — 15-d swaps every
  snprintf call site in one atomic commit; same dual-buffer
  rationale as Step 14-b/c (mixed snprintf + dtoa outputs would
  diverge on borderline inputs where libc and Ryū disagree on
  the shortest-roundtrip representation).

## ROI estimate

- **Perf**: Ryū is 2-3× faster than libc's `%g`-with-try-precisions
  on average f64. Print-heavy fixtures with f64 output
  (mandelbrot, ackermann's float branches if any, plus any
  f64-containing fixture) see direct speedup. Geomean impact
  ≈ +1-3% on vs-bun-aot ratio.
- **Binary size**: each user binary loses the libc `_snprintf` +
  `_strtod` dyld stub entries; saves ~100-200 B per binary. More
  importantly, after A5 the user binary may drop libSystem
  entirely (no libc imports at all), saving a multi-MB dyld
  resolve overhead at startup.
- **Correctness**: Ryū is shortest-roundtrip BY CONSTRUCTION; the
  current snprintf try-precisions loop is **stable but slower**
  and divergent across libcs (musl vs glibc vs Apple libc round
  edge-case f64 inputs differently). torajs-fmt eliminates that
  cross-platform drift.

## Acceptance status

- 15-a (crate scaffold): 🟡 ready to ship.
- 15-b (Ryū dtoa): 🟡 blocked on 15-a.
- 15-c (Eisel-Lemire atod): 🟡 blocked on 15-a (parallel with 15-b).
- 15-d / 15-e (call site cutover): 🟡 blocked on 15-b / 15-c.
- 15-f (optional): 🟡 deferred to A5 audit.

## Pre-flight (this audit doc-only commit)

- 1 file added (this finding doc, `docs/v0.7-A4-finding.md`).
- No substrate change; no conformance gate required per A-track
  example #1 (docs-only).
- File-size: this finding ~280 LOC < 500 hard limit ✓.

## Related

- [`v0.7-A3-finding.md`](v0.7-A3-finding.md) — A3 phase finding
  with full putchar / printf cutover ladder (Step 14-a/b/c).
- [`v0.7-A2-finding.md`](v0.7-A2-finding.md) — A2 phase finding
  (Step 12-13-a perf ladder).
- [`roadmap.md`](roadmap.md) — v5 三轴 trunk with A 轴 ladder
  (A2 → A3 → **A4 active** → A5 → A 轴 close).
- [`deps-tree-v0.1.md`](deps-tree-v0.1.md) — A 族 stone / cement
  split plan; torajs-fmt slots into the same metal-tier pattern
  as torajs-syscall / torajs-io / torajs-mmalloc.
- Ryū reference: Adams, "Ryū: Fast Float-to-String Conversion",
  PLDI 2018 — algorithm canonical citation.
- Eisel-Lemire reference: Lemire, "Number Parsing at a Gigabyte
  per Second", Software: Practice and Experience, 2021 —
  algorithm canonical citation.
- [`../crates/torajs-syscall/src/safe.rs`](../crates/torajs-syscall/src/safe.rs) —
  pattern reference for "0-libc Rust crate using only `core` +
  raw syscalls" (torajs-fmt has even simpler shape since no
  syscalls needed).
